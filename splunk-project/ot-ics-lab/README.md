# OT/ICS Detection Lab

## Detection Engineering Project

**Analyst:** Santiago Abel Ruiz Diaz
**Platform:** GRFICSv2 (Tennessee Eastman process simulation) · OpenPLC v2 · ScadaBR HMI · Wazuh 4.12.0 EDR · Splunk Enterprise 10.4.0 · Zeek NSM (icsnpp-modbus)
**Status:** Infrastructure built and verified end-to-end · Attack simulation and detection validation — in progress

A Purdue-model ICS segment added to the home SOC lab on its own isolated VLANs — a process simulation, a soft PLC, and a supervisory HMI wired together the way a real Level 0–2 OT environment is, not a single flat network.

---

## Lab Infrastructure — Purdue Model

```
  ╔═══════════════════════════════════════════════════════════════╗
  ║  Level 2 — Supervisory Control        VLAN 55 (OTDMZ)         ║
  ║  ScadaBR HMI      10.10.55.10   pve1                          ║
  ║  Operator view, historian, isolated DMZ                       ║
  ╚═══════════════════════════╦═══════════════════════════════════╝
                              ║ Modbus TCP :502 (OTDMZ → ICSNETWORK, allowed)
  ╔═══════════════════════════╩═══════════════════════════════════╗
  ║  Level 1 — Basic Control               VLAN 50 (ICSNETWORK)   ║
  ║  OpenPLC v2       10.10.50.20   pve3   Soft PLC, Modbus master║
  ╠═════════════════════════════════════════════════════════════════╣
  ║  Level 0 — Field Devices / Process                             ║
  ║  grfics-sim       10.10.50.10-15   pve3   Sensors, actuators   ║
  ║  Tennessee Eastman chemical process simulation (Unity WebGL)   ║
  ╚═══════════════════════════════════════════════════════════════╝
```

Reference build: [GRFICSv2](https://github.com/Fortiphyd/GRFICSv2). Detection rules (below) adapted from [rudrasecure/otsoctraining](https://github.com/rudrasecure/otsoctraining).

---

## Verified Operation

The full Level 0 → 1 → 2 Modbus chain is live and confirmed working end-to-end: field-device values generated by the process simulation flow through the soft PLC and surface correctly on the supervisory HMI.

| ScadaBR HMI (Level 2) | grfics-sim process view (Level 0) | OpenPLC (Level 1) |
|---|---|---|
| [screenshot](screenshots/normal-01-scadabr-hmi-baseline.png) — live Feed1/Feed2/Purge/Product values | [screenshot](screenshots/normal-03-grfics-sim-baseline.png) — 1478 kPa, 53% level | [screenshot](screenshots/normal-02-openplc-log.png) — Modbus master connected to all 6 field devices |

---

## Attack Scripts & Detection Rules — written, not yet run

Attack scripts and a first set of Wazuh detection rules are written and staged, ready for the next phase, but nothing has been run against this build yet — no attack results are claimed here.

- [`attack-scripts/modbus_attack.py`](attack-scripts/modbus_attack.py) — Modbus FC16 WRITE to setpoint registers 0–9 and pressure safety override registers 1025/1026 (T1855)
- [`attack-scripts/shutdown_plant.py`](attack-scripts/shutdown_plant.py) — coil 40 write, emergency shutdown interlock (T1855)
- [`wazuh/local_rules.xml`](wazuh/local_rules.xml) — PLC pressure-value monitoring, FIM on the PLC program directory, unauthorized-source Modbus access

**Two issues found reviewing the rules before deployment:**

1. Two of the rules depend on `if_sid 86601` (Wazuh's native Suricata EVE-JSON alert rule), which requires a Wazuh agent reading `eve.json` directly. The rest of this lab routes Suricata alerts via OPNsense syslog → **Splunk** instead (see `credential-attack-detection/` and `ad-privesc-lab/`), so as written these two rules won't fire against this lab's actual log flow — needs a pipeline decision before deploying.
2. The PLC pressure-value rule parses a `value` field out of `/var/log/output.log` with no accompanying decoder to extract it — inherited from the upstream project, which has the same gap. Needs a live check against the actual log format.

---

## Roadmap

- Run the attack scripts above against this build, capture Splunk/Wazuh/Zeek evidence, and document results in a dedicated follow-up
- Resolve the two rule issues above and deploy to the Wazuh manager
- BACnet track: a second, independent OT protocol build (7-device HVAC/BMS simulator on VLAN 60, `bacnet-sim`, read access already confirmed via Zeek's `icsnpp-bacnet`) — no attack or detection designed yet
- Suricata ET SCADA ruleset on the OPNsense ICSNETWORK interface
